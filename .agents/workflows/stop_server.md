---
description: Cómo detener el servidor web localmente
---

Para detener el servidor web de Spring Boot y liberar el puerto 8080 debes invocar el script de PowerShell de la raíz del proyecto.

1. Ejecuta el script de detención en Foreground:
// turbo
```powershell
.\stop-server.ps1
```
